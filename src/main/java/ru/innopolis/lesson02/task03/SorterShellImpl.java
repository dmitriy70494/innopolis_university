package ru.innopolis.lesson02.task03;

import java.util.Comparator;

public class SorterSheelImpl implements Sorter {
    @Override
    public Person[] sortedArray(Person[] persons, Comparator<Person> comparator) {

        return new Person[0];
    }
}
